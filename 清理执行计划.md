# 清理执行计划

## 已完成的准备工作

✅ 1. **分析代码库结构**
   - 识别了备份文件、临时文件、重复脚本
   - 确定了不需要的文件清单
   - 计算了可释放的磁盘空间（约14MB）

✅ 2. **创建清理脚本**
   - `cleanup-project.sh` - 本地开发环境清理脚本
   - `cleanup-server.sh` - Linux服务器专用清理脚本
   - 两个脚本都有安全确认机制

✅ 3. **创建文档**
   - `CLEANUP-README.md` - 详细使用说明
   - 更新了 `.gitignore` 文件

✅ 4. **设置权限**
   - 脚本已添加执行权限

## 服务器清理步骤

### 第一步：备份（重要！）
```bash
# 在服务器上备份当前项目
cd /var/www/food-subscription-v01.1-backup
tar -czf backup-$(date +%Y%m%d-%H%M%S).tar.gz .
```

### 第二步：运行清理脚本
```bash
# 进入项目目录
cd /var/www/food-subscription-v01.1-backup

# 先查看将要删除的文件（干运行模式）
bash cleanup-project.sh --dry-run

# 如果确认无误，运行服务器清理脚本
sudo bash cleanup-server.sh
```

### 第三步：恢复依赖和构建
```bash
# 重新安装后端依赖
cd /var/www/food-subscription-v01.1-backup/backend
npm install --production

# 重新安装前端依赖
cd /var/www/food-subscription-v01.1-backup/frontend-src
npm install

# 重新构建前端
cd /var/www/food-subscription-v01.1-backup/frontend-src
npm run build

# 复制构建文件到生产目录
cp -r /var/www/food-subscription-v01.1-backup/frontend-src/dist/* \
      /var/www/food-subscription-v01.1-backup/frontend/dist/
```

### 第四步：重启服务
```bash
# 重启后端服务
sudo systemctl restart food-subscription

# 重启Nginx
sudo systemctl restart nginx

# 检查服务状态
sudo systemctl status food-subscription
sudo systemctl status nginx
```

## 预期清理结果

### 将删除的文件：
1. **备份文件** (15KB)
   - `backend/db/init-mysql.js.backup`

2. **临时文件** (≈1.7MB)
   - `food-subscription-v1.1.zip`
   - `my-app@0.0.0`
   - `test_ssh.txt`
   - `backend/package-lock.json`

3. **重复脚本** (≈10KB)
   - `deploy-v1.1.sh` (保留 `deploy.sh`)
   - `fix-line-endings-v1.1.py` (保留 `fix-line-endings.py`)
   - `fix-v1.2.ps1`, `fix-v1.2.sh` (保留 `fix-v1.2.py`)

4. **重复logo文件** (≈285KB)
   - `logo.png`, `logo-256.png`, `logo-optimized.png`
   - 保留：`logo-512.png` (最大), `logo-128.png` (最小)

5. **构建产物** (≈3MB)
   - `frontend/dist/`
   - `frontend-src/dist/`

6. **依赖目录** (≈9.5MB)
   - `backend/node_modules/`
   - `frontend-src/node_modules/`

### 总计：约14MB磁盘空间

## 注意事项

### ⚠️ 重要警告
1. **备份优先**：清理前务必备份
2. **服务中断**：清理期间服务可能短暂中断
3. **依赖恢复**：删除node_modules后需要重新安装依赖
4. **构建恢复**：删除dist目录后需要重新构建

### 🔧 故障排除
```bash
# 如果npm安装失败（网络问题）
npm config set registry https://registry.npmmirror.com

# 如果构建失败
node --version  # 确保Node.js >= 18

# 如果权限问题
sudo chown -R www-data:www-data /var/www/food-subscription-v01.1-backup
```

### 📊 验证清理效果
```bash
# 检查是否还有备份文件
find /var/www/food-subscription-v01.1-backup -name "*.backup" -o -name "*.bak"

# 检查磁盘空间变化
df -h /var/www/
du -sh /var/www/food-subscription-v01.1-backup
```

## 后续维护建议

### 定期清理
```bash
# 每月执行一次清理
0 2 1 * * cd /var/www/food-subscription-v01.1-backup && bash cleanup-server.sh
```

### 版本控制
```bash
# 清理后提交更改
cd /var/www/food-subscription-v01.1-backup
git add .
git commit -m "清理项目：删除不需要的文件"
git push origin main
```

### 监控磁盘空间
```bash
# 添加磁盘监控
df -h /var/www/ | grep -v Filesystem
```

---

**执行状态**：✅ 脚本已准备就绪
**预计耗时**：10-15分钟（包括依赖安装和构建）
**风险等级**：中等（需要备份和确认）
**推荐执行时间**：低流量时段（如凌晨2-4点）

**最后检查**：2026-02-28
**脚本版本**：1.0
**适用环境**：Ubuntu 20.04+ / CentOS 7+